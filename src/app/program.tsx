import { ProgramEditorScreen } from '@/features/program/ProgramEditorScreen';

// Pushed stack screen (root Stack, headerShown: false). Reached from Settings ▸ Program.
export default function ProgramRoute() {
  return <ProgramEditorScreen />;
}
